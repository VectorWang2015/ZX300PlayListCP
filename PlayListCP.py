import os
import os.path
import re

def readPlayListFile(fd):
    """read a file descriptor, return a list of lines with relative paths of songs"""
    pass


def processSong(song_path, new_path):
    """parse song_path, cp the song to the new_path"""
    pass
